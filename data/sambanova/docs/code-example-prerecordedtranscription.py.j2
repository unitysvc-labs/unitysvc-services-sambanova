import os
import requests

UNITYSVC_API_KEY = os.getenv("UNITYSVC_API_KEY")
UNITYSVC_BASE_URL = os.getenv("UNITYSVC_BASE_URL")

url = f"{UNITYSVC_BASE_URL}/audio/transcriptions"
headers = {"Authorization": f"Bearer {UNITYSVC_API_KEY}"}

files = {
    "file": ("obamacompressed.mp4", open("obamacompressed.mp4", "rb")),
    "model": (None, "{{offering.name}}")
}

response = requests.post(url, headers=headers, files=files)

print("Status:", response.status_code)
print(response.text)
