name: Format Check

on:
  workflow_dispatch:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches: [main]

jobs:
  format-check:
    uses: unitysvc-labs/.github/.github/workflows/seller-format-check.yml@main
